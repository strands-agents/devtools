apiVersion: 1

datasources:
  - name: GitHub Metrics (SQLite)
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true
    jsonData:
      path: /var/lib/grafana/data/metrics.db
      mode: ro
